import { FormFieldValidationPipe } from './form-field-validation.pipe';

describe('FormFieldValidationPipe', () => {
  it('create an instance', () => {
    const pipe = new FormFieldValidationPipe();
    expect(pipe).toBeTruthy();
  });
});
